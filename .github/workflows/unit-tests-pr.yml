name: "Unit Tests"

on:
  pull_request:
    types:
      - opened
      - synchronize
  push:
    branches:
      - main # so that test reports get uploaded to Codecov and SonarCloud
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frontend-unit-tests:
    if: (github.actor != 'dependabot[bot]' && github.actor != 'renovate[bot]')
    name: Unit Tests
    uses: ./.github/workflows/frontend-unit-tests.yml
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}

  explorer-unit-tests:
    if: (github.actor != 'dependabot[bot]' && github.actor != 'renovate[bot]')
    name: Explorer Unit Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: explorer
    steps:
      - uses: actions/checkout@v6
      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm ci --ignore-scripts
      - run: npm run test:fee-accounting
      - run: npm run lint
      - run: npm run build

  backend-unit-tests:
    if: (github.actor != 'dependabot[bot]' && github.actor != 'renovate[bot]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r backend/requirements.txt
      - run: pip install pytest-cov pytest-xdist
      - run: pytest tests/unit --cov=backend --cov-report=xml --cov-branch -n auto

      # Backend coverage.xml previously had exactly one consumer (SonarCloud).
      # Codecov measures the actual PR diff rather than a stale branch baseline.
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5.5.2
        with:
          verbose: true
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          files: coverage.xml
          flags: backend
